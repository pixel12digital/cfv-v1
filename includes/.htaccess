# ==============================================
# PROTEÇÃO DO DIRETÓRIO /includes/
# Bloqueia acesso direto via HTTP
# Estes arquivos devem ser incluídos via PHP, não acessados diretamente
# ==============================================

# Negar acesso a todos via HTTP
<IfModule mod_authz_core.c>
    # Apache 2.4+
    Require all denied
</IfModule>

<IfModule !mod_authz_core.c>
    # Apache 2.2
    Order deny,allow
    Deny from all
</IfModule>
